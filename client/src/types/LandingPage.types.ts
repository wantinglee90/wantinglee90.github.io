export interface LandingPageProps {
  leftImage: string;
  rightImage: string;
  leftOverlayColor: string;
  rightOverlayColor: string;
  leftTitle: string;
  rightTitle: string;
  leftSubtext: string;
  rightSubtext: string;
}